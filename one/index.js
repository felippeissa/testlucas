const test = require("./testCases");

function one(array) {
  var soma1 = 0;
  var arreio = array.map();
  for (var a = 0; a < array.length(); a++) {
    var soma1 = arreio[a] + soma1;
  }

  return resultado;
}

test(one);
