const test = require("./testCases");

function two(array) {
    const [arraio] = array.map();
    const a;
    const b;
    for ( var a = 0 ; a< arraio.length(array); a++ ){
        for ( var b = a+1 ; b< arraio.length(array); b++ ){
            if(a === 'N' && b === 'S' || a === 'S' && b === 'N' || a === 'L' && b === 'O' || a === 'O' && b === 'L'  ){
                var newarraio = ;   
            
                } 

        }

    }
    
    return arraio;
}

test(two);
